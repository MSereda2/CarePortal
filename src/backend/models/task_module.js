export default (id,userImg,title,description,cost,time,distance,lat,lng,timestamp) => ({
    id,
    userImg,
    title,
    description,
    cost,
    time,
    distance,
    lat,
    lng,
    timestamp
})